services:
  app:
    image: &app ghcr.io/fredagscafeen/web:latest
    restart: always
    networks:
      - traefik
      - default
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - ./media:/app/media
      - ./django.log:/app/django.log
      - ./.env:/app/.env
      - /var/run/cups:/var/run/cups
    labels:
      - traefik.enable=true
      - traefik.http.routers.fredagscafeen.rule=Host(`fredagscafeen.dk`)
      - traefik.http.routers.fredagscafeen.entrypoints=https
      - traefik.http.routers.fredagscafeen.tls=true
      - traefik.http.routers.fredagscafeen.tls.certresolver=lets-encrypt
      - traefik.http.services.fredagscafeen.loadbalancer.server.port=8000
    depends_on:
      - db
      - media
      - redis

  celery:
    image: *app
    restart: always
    command: ["celery", "--app=fredagscafeen", "worker", "--loglevel=INFO"]
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - ./.env:/app/.env
    depends_on:
      - app
      - redis
      - db

  celery-beat:
    image: *app
    restart: always
    command: ["./wait_for_app", "celery", "--app=fredagscafeen", "beat", "--loglevel=INFO", "--scheduler=django_celery_beat.schedulers:DatabaseScheduler"]
    volumes:
      - ./.env:/app/.env
    depends_on:
      - app
      - redis
      - db

  db:
    image: postgres:18
    restart: always
    ports:
      - "5432:5432"
    volumes:
      - ./db_data:/var/lib/postgresql
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=sehhik-9sysfa-wIkxaq
      - POSTGRES_DB=db

  media:
    image: nginx:alpine
    restart: always
    volumes:
      - ./media:/usr/share/nginx/html
    networks:
      - traefik
    labels:
      - traefik.enable=true
      - traefik.http.routers.media-fredagscafeen.rule=Host(`media.fredagscafeen.dk`)
      - traefik.http.services.media-fredagscafeen.loadbalancer.server.port=80
      - traefik.http.routers.media-fredagscafeen.tls.certresolver=lets-encrypt

  redis:
    image: redis:alpine
    restart: always

networks:
  traefik:
    external: true
